{# This is the page that will display the charging stations' catalog #}

{% extends 'front/front_layout.html.twig' %}

{% block title %}Bornes de Ravitaillement - {{ parent() }}{% endblock %}
{% block body %}
    <h1 class="stationpage-title">Nos Solutions de Ravitaillement </h1>
    <p class="presentation-text">
        Faire le plein de Gaz Naturel n'a jamais été aussi facile grâce à la borne de recharge installée à domicile ou sur le lieu de travail.
        Une fois connectée sur votre réseau domestique, la borne de recharge vous permet ravitailler votre véhicule : la nuit suffit à faire le plein !
    </p>
    {% for refillStation in refillStations %}
        <h2 class="station-title">{{refillStation.name}}</h2>
        <p class="station-description">{{ refillStation.description }}</p>
        <div class="station-block">
            <div>
                <img class="stations-image" src="/assets/images/uploads/{{refillStation.photo}}" alt="{{refillStation.name}}">
            </div>
            <div>
                <h3 class="station-details-title">Caractéristiques de la station</h3>
                    <table class="table-show-station table table-hover">
                    <tbody>
                        <tr>
                            <th scope="col">Débit</th>
                            <td>
                            {% if refillStation.debit is not empty %}
                                {{ refillStation.debit }} m3 / h
                            {% else %}
                                Non Renseigné
                            {% endif %}
                            </td>
                        </tr>
                        <tr>
                            <th scope="col">Lieu d'installation</th>
                            <td>
                                {% if refillStation.installation is not empty %}
                                    {{ refillStation.installation }}
                                {% else %}
                                    Non Renseigné
                                {% endif %}
                            </td>
                        </tr>
                        <tr>
                            <th scope="col">Temps de recharge</th>
                            <td>
                                {% if refillStation.refillTime is not empty %}
                                    {{ refillStation.refillTime}} heures
                                {% else %}
                                    Non Renseigné
                                {% endif %}
                            </td>
                        </tr>
                        <tr>
                            <th scope="col">Stokage supplementaire</th>
                            <td>
                                {% if refillStation.additionalStorage is not empty %}
                                    {% if refillStation.additionalStorage %}Oui{% else %}Non{% endif%}
                                {% else %}
                                    Non Renseigné
                                {% endif %}
                            </td>
                        </tr>
                    </tbody>
                    </table>
                    <a href="{{path('contact_estimate')}}"><input class="station-request-button" type="button" value="Demande de devis"></a>
            </div>
        </div>
    {% endfor %}
{% endblock %}
