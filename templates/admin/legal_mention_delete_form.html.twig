<form method="post" action="{{ path('admin_legal_delete', {'id': legalMention.id}) }}" onsubmit="return confirm('Etes vous sur de vouloir supprimer cet élement?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ legalMention.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>
